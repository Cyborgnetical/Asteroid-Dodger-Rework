//@ts-check
export default {
    /**
     * 
     * @param {number} min 
     * @param {number} max 
     * @returns {number} random number between min and max
     */
    rando(min,max){
        return Math.random() * (max - min) + min;
    },
    /**
     * @param {number} angle
     */
}